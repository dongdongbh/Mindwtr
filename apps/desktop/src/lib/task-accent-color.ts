// Moved to packages/core (one home for identity colours, shared with the
// Android widget); re-exported so desktop imports stay put.
export { getAccentTint, getProjectAccentColor, getTaskAccentColor } from '@mindwtr/core';
