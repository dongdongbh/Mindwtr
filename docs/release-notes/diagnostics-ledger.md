# Release diagnostics ledger

Maintainer file, not user docs. Every stable or RC release adds one log line per change that field testers are asked to confirm, and removes the previous release's lines once their issues are confirmed or closed; unconfirmed lines are carried forward under the new version. Complicated fixes add their line in the fix commit (AGENTS.md "Diagnostic log rides complicated fixes"); simple UI fixes need none; the release pass (`publish-release`, `publish-rc-release`) adds the rest and does the trim.

Convention: a release-specific line carries `extra.releaseCheck = "<version>/<slug>"` so it can be found with `git grep -n releaseCheck` and trimmed by version. General diagnostics (the sync trail, `[sync-encryption]` events, `Mobile background sync started`/`finished`, activation proofs) never carry `releaseCheck` and are never trimmed.

## General (keep)

- Sync trail: `Sync start`, `Sync step`, `Sync read check found no changes`, `Sync diagnostic complete` (core `sync-run.ts`, both apps). `Sync cycle requeued` carries `reason` and, since #1170, `detail` = the transport's own message (for a WebDAV 412: `WEBDAV_REMOTE_WRITE_CONFLICT: WebDAV document changed before replacement (412)`), so a server rejecting its own ETag shows in one line.
- `[sync-encryption]` state / remote-read / transition / error / activation (core + both apps).
- `Mobile background sync started` / `finished` / `run took longer than a minute` (`apps/mobile/lib/background-sync-task.ts`, #1001).
- `Mobile background sync registered` with interval (same file).
- `Mobile background sync registration checked` with `decision` (register | re-register | unchanged | unregister | deferred-until-foreground | waiting-for-background-run), `registered`, `storedInterval`, `interval`, `appState` (same file; a headless cold start must log `deferred-until-foreground`, never a re-register). Promoted from v1.2.7 after ltguillaume confirmed the background-job fix on 1.2.7 (2026-09-03).
- Desktop `Sync backend selected; running the verification sync to activate it` (`useSyncSettings.ts`).
- Desktop reminders: `Desktop reminder fired`, `Desktop notification sent`, and `Desktop notification send failed` (`notification-service.tsx`). These content-free lines separate scheduler activity from the selected delivery path. Promoted after #1146 was confirmed on 1.2.8 and closed.
- `Sync store reconcile replaced tasks for an attachment-only change` (`store-settings.ts`), with entity and count but no content or identifiers. Promoted after the repeated-sync fix was confirmed in #1136 and #1156 on 1.2.8.
- `Remote sync mutation fence renewed for mutation horizon` (`sync-remote-fence.ts`), with the requested horizon and conservative remaining authority. This remains useful when a provider mutation later loses its fence.
- `Capture webhook request accepted` (`server-capture.ts`), with request shape, size, audio presence, and token scope but no content, identifier, or secret. This remains the server-side proof that a capture-only client reached the webhook.
- `Linux desktop notification submitted` / `Linux desktop notification delivery failed` (`notification-service.tsx`), with backend, bounded outcome, and a safe error category on failure.
- `Android widget list rendered within parcel budget` (`widget-service.ts`), with aggregate collection, item, eligible-item, and parcel-byte counts.

## v1.3.3 (add before tagging, trim in the release after)

- **`v1.3.3/encrypted-remote-recovery`** — desktop `sync-service.ts` and mobile `sync-service.ts`, after a verified WebDAV encrypted-document read durably restores a matching stale `remote-plaintext` state to `enabled` (#1293). Message: `Verified encrypted remote cleared stale plaintext state`. The next automatic sync must proceed rather than report plaintext suppression; the marker alone proves the local state repair, not a completed sync. No remote location, task content, or encryption material is logged.

- **`v1.3.3/restore-snapshot-sync`** — desktop `apps/desktop/src/lib/sync-service.ts`, after the restored snapshot is prepared with fresh sync revisions, saved exactly, and reloaded. Message: `Recovery snapshot restore committed`; field: `releaseCheck`. The tester restores a recovery snapshot, confirms its tasks stay live after sync with another device, and checks that a second sync leaves them unchanged. A failed read, save, or reload emits no success line. No task content, identifiers, or snapshot paths are logged.

- **`v1.3.3/ios-external-calendar-open`** — mobile `apps/mobile/components/views/calendar/useCalendarViewController.ts`, when Expo Calendar reports completion of the iOS native event editor action. Message: `Native calendar event dialog completed`; field: `releaseCheck`. The tester must also confirm Mindwtr responds to taps after the editor disappears. A missing line after dismissing the editor calls for another diagnostic log. No event content or identifiers are logged.

- **`v1.3.3/android-task-list-filter-render`** — mobile `apps/mobile/components/task-list.tsx`, once per mounted Android task list when filtering reduces a list that had clipping enabled to fewer than 15 rows. Message: `Android task list clipping retained after filtering`; fields: `releaseCheck`, `count`. The tester searches for a task near the end of a long Reference or other task list; the log must show `count` below 15 and the matching row must be visible without first scrolling the unfiltered list. No task content or identifiers are logged.

- **`v1.3.3/cloudkit-large-library-write`** — desktop and iOS `cloudkit-sync.ts`, after all record types, purged-record deletes, and the change-token step complete without a transport error. Message: `CloudKit write complete`; fields: `releaseCheck`, `conflicts`. A line with zero conflicts proves this build completed the serialized, unchanged-record-filtered write path; confirm the other device receives edits and both devices show a successful sync. A missing line or nonzero conflicts requires another diagnostic log. No task content or identifiers are logged.

- **`v1.3.3/cloudkit-retry-hint`** — desktop and iOS `cloudkit-sync.ts`, when a failed native CloudKit operation exposes a retry interval. Message: `CloudKit retry interval received`; fields: `releaseCheck`, `operation`, `retryAfterMs`. The tester's log must show the interval on the failed operation, followed by a later automatic sync attempt no earlier than that interval; the line alone proves only that the hint crossed the native bridge. No task content, identifiers, paths, or raw errors are logged.

- **`v1.3.3/purge-refused-outside-trash`** — core `packages/core/src/store-tasks.ts`, when Delete forever (desktop or mobile Trash) is asked to purge a task that is no longer in Trash, because sync or another device restored it after the confirmation. Message: `Purge refused for a task not in Trash`; fields: `releaseCheck`, `purged` (true if it was already purged). The log proves the live task was kept. No task content or identifiers are logged.
- **`v1.3.3/scheduled-focus-queue`** — core `packages/core/src/store-tasks.ts`, after a desktop or mobile task create or update accepts a future-start Next action's Focus star. Message: `Scheduled Focus queued`; fields: `releaseCheck`, `operation` (`create` or `update`), and `count` for batch creation. The log proves the queued star was accepted into the store; the task must remain in Upcoming until its start day, then appear in Today's Focus. It does not prove the pending save has reached disk. No task content or identifiers are logged.

- **`v1.3.3/scheduled-focus-cloud-write`** — cloud `apps/cloud/src/server.ts`, after a cloud task create or patch queues a future-start Next action for Focus. Message: `Cloud task Focus write policy applied`; fields: `releaseCheck`, `operation` (`create` or `patch`), `outcome` (`queued`), `count` (current Focus slots, unchanged by the queue). The log proves the cloud accepted the queued star without consuming today's limit. No task content or identifiers are logged.

- **`v1.3.3/sandbox-settings`** — desktop `apps/desktop/src/components/views/SandboxSettingsView.tsx` and mobile `apps/mobile/components/settings/sandbox-settings-screen.tsx`, after a sandbox setting is applied through the current workspace store and its pending save is flushed. Message: `Sandbox setting applied`; fields: `releaseCheck`, `setting` (`editor-opening`, `editor-layout`, `density`, `text-size`, `task-age`, `week-start`, `date-format`, or `time-format`), `platform` (`desktop` or `mobile`). The tester changes a setting in the sandbox, confirms the app responds, exits, and confirms the personal setting is unchanged. No personal setting value or task content is logged.

- **`v1.3.3/side-peek-unsaved-exit`** — desktop `apps/desktop/src/components/TaskItem.tsx`, when the user chooses Save, Discard, or Stay in the unsaved task editor prompt. Message: `Task editor exit decided`; fields: `releaseCheck`, `outcome` (`saved`, `discarded`, or `stayed`). A `saved` line follows a successful durable flush; a failed save leaves the prompt open and logs no decision. Confirm by editing a task title, then switching tasks, views, projects, and closing Weekly Review: each path must offer the same choices, and Stay must preserve the draft. No task content or identifier is logged.

- **`v1.3.3/skip-recurring-occurrence`** — core `packages/core/src/store-tasks.ts`, after a fixed-schedule occurrence has been archived without completion and its next occurrence, if any, has been saved durably. Message: `Recurring occurrence skipped`; field: `context.releaseCheck`. In Diagnostics, the line proves the skip write finished. Confirm on screen that the next scheduled task appears, the skipped one is not counted as completed, and a series ending at COUNT or UNTIL creates no extra task. No task content or identifier is logged.

- **`v1.3.3/monthly-weekday-position`** — core `packages/core/src/recurrence.ts`, when completing or skipping a monthly Mon–Fri positioned occurrence generates the next task. Message: `Monthly weekday recurrence advanced`; fields: `releaseCheck` and `position` (`1`–`4` or `-1`). The line proves the shared recurrence calculation ran; confirm the next date on screen and after syncing another device. It does not prove the pending save reached disk. No task content or identifier is logged.

- **`v1.3.3/native-android-dev-task-command`** — native Android dev shell `apps/android-native/bundle/host-entry.ts`, after a task or project command returns from the shared core save path, or when the contract refuses the command. Message: `Native Android task command`; fields: `extra.releaseCheck`, `operation` (`quickCapture` (the capture popup's Save, through core's `submitQuickCapture`), `quickCaptureLines` (its several lines, `submitQuickCaptureLines`), `quickCapturePicker` (a project or area its picker creates), `complete`, `update` (status menu, or the Restore and Next swipes), `saveTaskDraft` (task editor save, through core's `saveTaskDraft`), `taskFocus` (task star), `projectFocus` (project star), `createProject` (Add new project), `areaFilter` (area switcher), `saveSearch` (global search's Save Search), `inboxCommit` (a Process Inbox step's answer: a choice, File it, Back, Create project, or the project search's Create), or `inboxSkip` (Process Inbox Skip)), `outcome` (`saved` or `failed`). A `saved` line proves the dev shell received durable acknowledgment; `failed` flags refusal for investigation. For `update`, `saveTaskDraft`, `saveSearch`, `inboxCommit`, `inboxSkip`, and the three capture operations, `failed` also covers a request core refused before writing (a conflict with another writer, an invalid field, or a Process Inbox session or item that changed). No task title or identifier is logged. This marker is for the isolated dev app; its tester diagnostics export is not yet wired.

- **`v1.3.3/native-android-dev-host-reuse`** — native Android dev shell `apps/android-native/android/app/src/main/java/tech/dongdongbh/mindwtr/pilot/ProcessCoreHost.kt`, logcat tag `MindwtrNativeDev`, when a recreated Activity (rotation, dark mode, window resize) or a new Inbox screen attaches to the process's already-running core host. Message: `Native Android host reuse`; fields: `releaseCheck`, `reason` (`activity-recreate` or `new-screen`), `activityAttach` (attach count for this screen), `inFlight` (a command was running), `boots` (host boots in this process; must stay `1`). Rotate the dev app while a capture saves: the log must show this line and only one `Core host boot started` line for the process. No task title or identifier is logged. This marker is for the isolated dev app; its tester diagnostics export is not yet wired.

- **`v1.3.3/native-android-legacy-json-ahead-guard`** — native Android `apps/android-native/android/app/src/main/java/tech/dongdongbh/mindwtr/pilot/core/LegacyRnStoreGuard.kt`, logcat tag `MindwtrNativeDev`, once per boot of a build that opens the React Native app's storage (today only the `upgradetest` harness build), before anything opens the RN database. Message: `Native Android legacy store guard`; fields: `releaseCheck`, `outcome` (`clear` or `blocked`), and on `blocked` a `reason`: `async-storage-unreadable` means the RN AsyncStorage file `databases/RKStorage` exists but could not be read; `json-too-large` means RN's JSON backup there is over 64 MB; `database-missing` means `files/SQLite/mindwtr.db` is gone while RN left other state and no JSON backup, so opening it would start an empty library; `database-unreadable` means a byte copy of the RN database and its `-wal` failed `quick_check`. The json-ahead marker no longer blocks: the host imports the backup (next entry). The guard reads only byte copies in the app's cache folder, never the original files. On `blocked` the app shows `Storage unavailable`, keeps every command disabled, and changes no file: no schema change, no `.prewrite` checkpoint, no task, no WAL checkpoint. The upgrade harness (`apps/android-native/scripts/check-upgrade-device.mjs`) must show `outcome=clear` after a normal RN install, and `database-missing` and `database-unreadable` after it injects the matching fault. No AsyncStorage names or values, task content, or paths are logged. This marker is for the isolated harness build; its tester diagnostics export is not yet wired.

- **`v1.3.3/native-android-legacy-json-import`** — native Android host `apps/android-native/bundle/host-entry.ts`, logcat tag `MindwtrNativeDev`, once per boot of the `upgradetest` harness build when core's `planLegacyJsonImport` acts on the RN app's AsyncStorage JSON backup, exactly as RN's next launch would. Message: `Native Android legacy JSON import` (a warning when `rnState` is `failed`); fields in `extra`: `releaseCheck`, `outcome` (`imported`: the backup was merged into SQLite, saved, and read back with every persisted field equal; `abandoned`: nothing was merged), `path` (`json-ahead`: RN left the marker `mindwtr-data:json-ahead-of-sqlite`, #964; `reconcile`: RN's one-time reconcile of a database with data; `migrate`: the database was empty or missing), `reason` on `abandoned` (`backup-missing`, `backup-corrupt`, or `backup-version`: not a version-2 backup), `rnState` (`updated`: the marker was deleted and/or the reconcile flag set in `RKStorage`, after a byte checkpoint in `files/SQLite/RKStorage.prewrite`; `unchanged`: no RN state change was needed; `failed`: that change or its checkpoint failed; the import stays in SQLite, the boot fails closed with `Storage unavailable` and makes no native write, and the next boot finds the import saved and retries only the `RKStorage` change), and on `imported` RN's own counts `backupTasks`, `sqliteTasks`, `mergedTasks`, `tasksFromBackup` (RN's count: backup-only tasks plus tasks whose merge tie resolved to the backup copy). Harness scenario 2 must log one line with `outcome` `imported`, `path` `json-ahead` and `rnState` `updated`, and its relaunch none; scenario 2b `abandoned`, `json-ahead`, `backup-corrupt`; scenario 5b `imported`, `migrate`. Counts only: no AsyncStorage values, task content, identifiers, or paths are logged. This marker is for the isolated harness build; its tester diagnostics export is not yet wired.

- **`v1.3.3/linux-portal-theme-signal`** — desktop `apps/desktop/src-tauri/src/ui.rs`, after a Linux portal color-scheme change is received and resolved. Message: `Linux portal theme change resolved`; fields: `releaseCheck`, `theme` (`light` or `dark`). Change the system appearance while Mindwtr uses System theme: the log must show the new theme and the app must follow it. This marker proves the signal path ran, not that the webview repainted. No task content or identifiers are logged. (#1270)

- **`v1.3.3/mobile-startup-writes-after-canonical-load`** — mobile `apps/mobile/hooks/root-layout/use-root-layout-pending-captures.ts`, once per launch, after the first pending-capture drain finishes. That drain now waits for the canonical SQLite load, not the backup snapshot that paints first. Message: `Startup capture drain ran after canonical data load`; fields: `releaseCheck`, `elapsedMs` (time from root-layout mount until that first drain finished), `count` (queue items ingested by it). Queue a capture while the app is closed (Android quick capture or an iOS Shortcut), then open the app: the log must show this line with `count` of at least 1, the capture must appear once, and every task saved before the app closed must still show. The same gate holds share intents, capture and context links, Pomodoro hydration, the Apple Reminders import, the Watch bridge, and a cold-start notification Complete action; this line proves only the capture drain. A second line with the same tag, `Canonical data re-fetched after a local change` (`apps/mobile/hooks/root-layout/use-root-layout-startup.ts`), appears only when a store write during the load (for example a tap on a row before the load finished) made core skip the SQLite result; fields: `releaseCheck`, `retryCount` (re-fetches, at most 2), `outcome` (`applied`, `skipped` or `failed`). `applied` proves the re-fetch recovered canonical data; `skipped` is a warning that the session may still run on the snapshot. Written only with debug logging on. No task content, identifiers or paths are logged.

- **`v1.3.3/sqlite-kept-omitted-live-rows`** — core `packages/core/src/sqlite-adapter.ts` (mobile and native Android), after a full save commits while it left in place live rows (no `deletedAt`) that the saved document omitted. Before this fix such rows were physically deleted when unchanged since the adapter's last read. The typical cause is a document built from a stale snapshot, such as the mobile startup snapshot saved by a quick-add before the SQLite load applied. Message: `SQLite save kept live rows the snapshot omitted` (warning); fields: `releaseCheck`, `table` (`tasks`, `projects`, `sections`, `areas`, `people` or `saved_filters`), `count` (rows kept). One line per table per save. A kept saved filter is also written back into the saved settings list. Omitted tombstones are still removed and are not counted; a tombstone that another row still references by foreign key waits until that row is gone. The line proves the guard ran; the kept rows must show again after the next load, and a later save of that loaded data must not log the line again. Written only with debug logging on. No task content or identifiers are logged.

## v1.3.2 (add before tagging, trim in the release after)

- **`v1.3.2/macos-microphone-capture`** — desktop `apps/desktop/src-tauri/src/audio.rs`, after a captured WAV is finalized. Message: `Audio capture WAV saved with sampled signal`; fields: `releaseCheck`, `total` (captured sample count), `count` (nonzero sample count). A positive `total` with zero `count` distinguishes a silence-only capture from an empty buffer; it does not prove microphone permission, audible input, or successful transcription. No audio content, paths, identifiers, device details, or raw errors are logged.

- **`v1.3.2/automation-capture-implicit-status`** — `scripts/mindwtr-automation-core.ts`, after durable task capture when status was omitted and a start or cancellation timestamp triggered core lifecycle inference. Message: `Automation capture applied implicit task status`; fields: `releaseCheck` only, on stderr. A start-only capture must persist as Next; a cancellation-only capture must persist as Archived with its timestamp retained. Explicit Inbox still wins and emits no implicit-status marker. No content, timestamps, identifiers, paths or credentials are logged.

- **`v1.3.2/automation-failed-operation-settled`** — `scripts/mindwtr-automation-core.ts`, after a failed operation successfully drains pending saves while its original profile adapter still owns the singleton store. Message: `Automation failed operation pending work settled`; fields: `releaseCheck` only, on stderr. A rejected task capture after project creation may retain the project in its original profile; the next profile must receive none of that snapshot. A terminal settlement failure retains the original operation error, emits no marker, and still cannot redirect queued data to another profile. No content, identifiers, paths or credentials are logged.

- **`v1.3.2/dictation-store-files-protected`** — desktop `apps/desktop/src/components/Task/useTaskItemAttachments.ts`, when draft reset or unmount suppresses deletion of a file generation observed in the store after the editor's initial baseline. Message: `Store attachment files protected during draft settlement`; fields: `releaseCheck` only, forced with debug logging off. The file must remain after cancelling retained dictation, while truly uncommitted draft files can still be cleaned. Durable save settlement clears this protection and retains normal deletion behavior. No content, identifiers or paths are logged.

- **`v1.3.2/cloud-quick-add-capture`** — `apps/cloud/src/server.ts`, after the single durable document write for `POST /v1/tasks`. Message: `Cloud quick-add capture saved`; fields: `releaseCheck` only. The task must retain parsed natural dates and link to an eligible existing or newly created project. Explicit props override parsed metadata; invalid dates and later validation failures persist neither task nor project and emit no success marker. The marker covers title-only capture too. No titles, identifiers, paths, tokens or raw input are logged.

- **`v1.3.2/widget-focus-pools`** — `apps/mobile/lib/widget-service.ts`, after successful native publication of a changed widget payload derived through the shared core Focus pools. Message: `Widget Focus pools published to native host`; fields: `releaseCheck` only. The log must show this marker after changed-payload publication, but not after fingerprint skips or failed publication. It proves delivery to the native host, not launcher redraw or physical-device background execution. No task content, identifiers or paths are logged.

- **`v1.3.2/attachment-replacement-held`** — desktop `apps/desktop/src/lib/sync-attachment-backends.ts` and mobile `apps/mobile/lib/attachment-sync-backends/cloud.ts`, after the third permanent self-hosted Cloud refusal for edited attachment bytes. Message: `Attachment replacement retained after upload refusal`; fields: `releaseCheck` only. The marker must appear once while the old cloud copy and edited local copy remain; pending replacement blocks candidate metadata publication, and later cycles make no upload attempt for that content identity. Changing content or restarting permits retry. No paths, identifiers, hashes, content, server response, or raw errors are logged.

- **`v1.3.2/file-generation-preserved`** — desktop `apps/desktop/src/lib/sync-attachment-backends.ts`, when an existing immutable File Sync generation fails plaintext digest verification. Message: `Preserved invalid File Sync attachment generation; refusing replacement`; fields: `releaseCheck` only. After the verification failure, the tester's log must show this line, the shared-folder generation must remain present, and the local attachment must remain available and pending without an upload-success marker. This proves fail-closed preservation, not which copy contains the intended bytes. No paths, identifiers, hashes, attachment content, or raw errors are logged.

- **`v1.3.2/automation-capture-project-routing`** — `scripts/mindwtr-automation-core.ts`, after quick-add project routing through the shared capture transaction and successful durable flush. Message: `Automation quick-add project routing completed`; fields: `releaseCheck`, `outcome` (`created`, `reused`, `explicit`). An unknown or archived-only name reports `created` with linked task/project in SQLite and `data.json`; an active match reports `reused`, and explicit project props report `explicit`. Invalid date input creates neither row and emits no success marker. No titles, identifiers, paths, credentials or raw input are logged.

- **`v1.3.2/automation-concurrent-write-retry`** — `scripts/mindwtr-automation-core.ts`, after a CLI/API operation detects a concurrent SQLite commit, reloads its profile and succeeds after one whole-operation replay. Message: `Automation storage operation succeeded after concurrent-write retry`; fields: `releaseCheck`, `retryCount` (`1`). Force an external commit between read and save: SQLite and `data.json` must retain both writers, and stderr must contain the marker. It proves the bounded replay succeeded, not that future non-cooperating writes are absent. No content, identifiers, paths, credentials or raw errors are logged.

- **`v1.3.2/automation-people-preserved`** — `scripts/mindwtr-automation-storage.ts`, stderr after JSON-backed people absent from SQLite are durably retained during automation migration. Message: `Automation people preserved during storage migration`; fields: `releaseCheck`, `count`. A people-only profile must emit the expected positive count and preserve every person and exact note in SQLite and the JSON mirror; stdout remains the CLI result. No names, notes, links, identifiers, paths, credentials, or raw errors are logged.

- **`v1.3.2/mcp-bootstrap-serialized`** — `apps/mcp-server/src/db.ts`, stderr after JSON fallback bootstrap resolves under the MCP cross-process write lock. Message: `MCP SQLite fallback bootstrap resolved`; fields: `releaseCheck`, `outcome` (`created` or `existing`). Two simultaneous MCP starts against a JSON-only profile must both succeed with one creator, retain imported records, and pass SQLite `integrity_check`. The marker proves serialized exclusive publication, not completion of an independently created winner's initialization. No task content, identifiers, paths, credentials, or raw errors are logged.

- **`v1.3.2/dictation-audio-retained`** — desktop `apps/desktop/src/components/Task/TaskItemFieldRenderer.tsx`, after failed transcription, accepted retained-audio metadata and a successful `flushPendingSave`. Message: `Dictation audio retained after task save completed`; fields: `releaseCheck` only. Proves the immediate Keep path crossed the task-save durability barrier, not playback, upload, sync or a later editor Save. A rejected update emits no success marker and leaves the recording in the attachment draft for Save retry. No text, bytes, paths or identifiers are logged.

- **`v1.3.2/cloudkit-push-delivered`** — mobile `apps/mobile/lib/cloudkit-sync.ts`, on the `onRemoteChange` listener (iCloud sync). The native handler `application(_:didReceiveRemoteNotification:fetchCompletionHandler:)` in `CloudKitSyncAppDelegateSubscriber.swift` returned `Bool` since March 2026, which does not match the system's optional requirement, so Expo never forwarded a push to it: a change made on another device reached the phone only at its next scheduled or foreground sync. Message: `CloudKit remote change notification received`; fields: `releaseCheck`, `origin` (`push`). Written even with debug logging off. The same subscriber now also calls `registerForRemoteNotifications()` on every launch: Apple delivers CloudKit's silent pushes only to an app that registered in that launch, and before this nothing registered unless the person had turned notifications on in Settings, so a device with iCloud sync on and notifications never enabled got no pushes at all. With iCloud sync on two Apple devices, change a task on one while the other is open: the second device's log must show this line within seconds, followed by a sync. Run it once on a device where notifications were never turned on. No line after a change means pushes still do not arrive (check the CloudKit subscription and the notification entitlement).

- **`v1.3.2/background-capture-drain`** — mobile `apps/mobile/lib/pending-capture-drain.ts` (#1257: a task added from the Android capture window never reached other devices until the app was opened). Logged after the capture queue was imported while the app was not visible. Message: `Queued captures imported in the background`; fields: `releaseCheck`, `trigger` (`capture` = started by **Save** in the capture window, `scheduled` = the periodic background sync), `count` (queue items imported). Close Mindwtr, add a task from the app-icon shortcut, the widget **+** or the Quick Settings tile, and do not open the app: the log must show this line with `trigger: capture`, followed by `Mobile background sync started` and `Mobile background sync finished` with `outcome: success`, and the task must appear on the other device. No line means the capture window could not start the background service; the task is then imported at the next app opening as before.

- **`v1.3.2/ios-control-capture`** — mobile `apps/mobile/app/+native-intent.ts` (feedback 4bf8d547: the iOS Control Center **Add Task** control did nothing). Logged when a quick-capture link tagged `source=control` is routed, which only the app-side `MindwtrOpenQuickCaptureIntent` (`apps/mobile/ios-app-intents/MindwtrSiriCaptureIntents.swift`) sends. Message: `Control Center quick capture routed`; fields: `releaseCheck`, `stage` (`capture-routed`), `delivery` (`cold` or `warm`). Written even with debug logging off. On an iPhone with iOS 18 or later, add the Mindwtr **Add Task** control to Control Center, tap it, then share the log: the line proves the tap ran the app's copy of the intent and reached the capture sheet. No line after a tap means iOS did not run the app-side intent (the fix did not take), whatever the screen showed. This code was never run on a device before release.

- **`v1.3.2/scheduled-inbox-promoted`** — `packages/core/src/store-load-migrations.ts` (#1248), on any load where the `promote-scheduled-tasks` pass rewrote a status, on desktop and mobile alike. Message: `Inbox tasks whose date arrived promoted to next during load migration`; fields: `releaseCheck`, `count` (how many tasks were rewritten). Written even with debug logging off, because the pass runs inside the load that reads the logging setting and an ordinary line there is dropped (a simulator run on 09-22 confirmed the line never appeared even for a task that was promoted). Inbox is now the only status this pass may touch: a Waiting, Someday, Reference, Done or Archived task whose start or due date has passed keeps its status. The check is that setting a task to **Someday** from the Focus screen survives the save, the sync and the next launch on every device, and that this marker does not appear for it — before the fix a dated Someday task was rewritten to Next on the very next load and that new revision was pushed to the other devices. A positive count is still expected and correct when an unprocessed Inbox task's date arrives. No task text, dates, statuses, or identifiers are logged.

- **`v1.3.2/standard-layout-subfolders`** — desktop `apps/desktop/src-tauri/src/storage_layout.rs` (#1245), once per start on the platforms where the OS resolves the app's config dir and data dir to the same folder (Windows Roaming, macOS Application Support). Message: `Storage layout migrated to profile subfolders`; fields: `releaseCheck`, `moved` (count of entries moved), `kept` (count of entries already at their destination, where the moved copy was kept and the root leftover left alone), `outcome` (`migrated` when a flat profile was moved into `config/` and `data/`, `already` when the profile was already split, `fresh` when the two folders were created for a new profile, `failed` when a move did not succeed or another process was already migrating the folder), and on failure `entry` (the fixed internal name of what failed: an entry such as `attachments` or `mindwtr.db`, or `lock` when another process held the migration lock, `subfolders` when the two folders could not be created, or `layout-migration.json` when the journal could not be written). On an installed build updated from an older version, the tester's first start must show `outcome=migrated` with a positive count, Settings > Data must show the data path ending in `data`, and every task, attachment, and sync setting must still be there; the next start must show `outcome=already` with `moved=0`. `failed` means the run put back everything it had moved, kept using the old flat folder, and nothing was lost — the migration is retried on the next start. The marker proves the folder move ran, not that any particular file was readable afterwards. Linux is unaffected and emits no marker, and portable installs are untouched. Downgrading to an older version is not supported after the move: it looks only at the flat folder. No user paths, file contents, task data, identifiers, or raw errors are logged.

- **`v1.3.2/apple-reminders-auto-import`** — mobile `apps/mobile/hooks/root-layout/use-root-layout-apple-reminders.ts` (#1238), after the opted-in Apple Reminders import has run on startup or on a foreground. Message: `Apple Reminders auto-import ran`; fields: `releaseCheck`, `imported`, `skipped`, `deleted`, `failed` (counts only). With **Import automatically** on and a list chosen, add a reminder to that list, then bring Mindwtr to the foreground: the log must show `imported=1` and the Inbox must hold the task; a second foreground within 30 seconds does not run again, and a later foreground with nothing new writes no line at all — a run that imported, deleted, and failed nothing is silent on purpose, so the absence of a line is the expected result, not a sign that the hook did not run. The marker never appears when the toggle is off, no list is chosen, or Reminders access was not granted (the hook never prompts). No reminder text, list names, or identifiers are logged.

- **`v1.3.2/font-family-applied`** — desktop `apps/desktop/src/App.tsx` (#1244), each time the chosen font is applied: once per start and once per change. Message: `Desktop font family applied`; fields: `releaseCheck`, `family` (the chosen family name, or `app-default` when the box is empty), `boldFace` (`real`, `synthesized`, `fallback`, `unknown`, or `app-default`). A font name carries no personal data. The fix it belongs to is in `apps/desktop/src-tauri/src/system_fonts.rs`: the **Font** list now leaves out families that have no face at weight 600 or heavier, because Chromium fakes the bold for those — it smears the regular face — and every bold label comes out doubled and blurry while regular text stays crisp. `boldFace` is the renderer's own answer, measured in `checkBoldFace` (`apps/desktop/src/lib/font-family.ts`): a faked bold is applied when a glyph is drawn and not when it is measured, so a family with no bold face measures the same at weight 400 and 700, while a real bold face measures wider. `fallback` means the family did not resolve at all and another font answered; `unknown` means the measurement could not run. The tester opens Settings > General > Look & feel > Font, picks a font, and shares the log: the line must name the font they picked with **`boldFace: real`**, and the sidebar labels, section headings, task titles and **Add task** must look as sharp as the rest of the text. `synthesized` on a font the list offered is the failure this fix is meant to make impossible — send the family name on, because it means the renderer disagreed with what the font files said. Fonts that were pickable before and are gone from the list are the ones that had no bold face; a font already chosen keeps working and is still shown in the box even when the list no longer offers it. Neither the list length nor `boldFace` was measured on Windows before release.

- **`v1.3.2/cloudkit-op-timeout`** — mobile `apps/mobile/lib/cloudkit-sync.ts`, when a native CloudKit call fails after running for at least 25 seconds. That floor sits just under the 30-second request timeout every `CKOperation` now carries (`CloudKitOperationTimeouts.apply` in `apps/mobile/modules/cloudkit-sync/ios/CloudKitOperationTimeouts.swift`, used by `CloudKitSyncManager.swift` and `CloudKitChangeTracker.swift`; the whole operation is capped at 180 seconds, 600 for an attachment transfer). Elapsed time is the only test, because no CloudKit error code crosses into JavaScript and the message that does is translated into the phone's language. A run the app cancelled itself is skipped — that is the wall-clock abort's line, below. Message: `CloudKit operation timed out`; fields: `releaseCheck`, `operation` (which call: `ensureZone`, `saveRecords:task`, `fetchAllRecords:project`, `fetchChanges`, …), `elapsedMs`. Written even with debug logging off. **What the log must show:** there is no way to trigger this on demand, so confirm it on a device that showed the long runs in the iPad report (v1.3.0/1.3.1, 2026-09-16..22: background runs of 2 to 35 minutes, three abandoned after 1977s/1364s/2067s). After this release, either those long runs stop, or, when a CloudKit call does fail slowly, this line appears with `elapsedMs` in the tens of seconds rather than the tens of minutes. No task content, record IDs, or raw CloudKit error text is logged.

- **`v1.3.2/background-sync-wallclock-abort`** — mobile `apps/mobile/lib/background-sync-task.ts`, when a background sync run passes its own 4-minute deadline (`MOBILE_BACKGROUND_SYNC_DEADLINE_MS`) and is abandoned. Message: `Mobile background sync did not finish before its deadline and was abandoned`; fields: `releaseCheck`, `elapsedMs`, `deadlineMs`, `stage`. Written even with debug logging off. `stage` says which of the two watchdogs noticed: `timer` is the run's own `setTimeout`, `resume` is the new `AppState` `'active'` listener, which catches a run whose timer was frozen while iOS had the app suspended (same iPad report as above). **Which one fires is a race the app does not control**, because React Native restarts the frozen timer at the same moment it delivers `'active'` — so treat either value as a pass; only a missing line is a failure. Both mark the run `outcome: abandoned` and arm the same failure cooldown. **What the log must show:** leave the app in the background well past 4 minutes with a sync in flight, then reopen it. This line must appear before `Mobile background sync finished`, and that run's `outcome` must read `abandoned`. No task content, credentials, or identifiers are logged.

- **`v1.3.2/widget-checkoff-serialized`** — Android `CheckoffStore.kt` records a count after a widget tap durably publishes pending state under the shared monitor; `AndroidWidgetModule.kt` drains it and `apps/mobile/lib/widget-service.ts` logs it on the next app-owned widget publication. Message: `Android widget check-off state serialized`; fields: `releaseCheck`, `count`. Enable Diagnostics, tap a Tasks-widget check-off, then open Mindwtr: a positive count proves eligibility revalidation and pending-state publication under serialization. It does not prove concurrent sweep execution, launcher rendering, queue ingestion, or task completion. No task content, identifiers, timestamps, queue names, or widget identifiers are logged.

- **`v1.3.2/reminder-live-import-guard`** — `apps/mobile/lib/apple-reminders-import.ts`, after a successful add and durable flush when the returned identity is missing or resolves only to a deleted/purged task. Message: `Apple Reminders import kept source because the resolved task is not live`; fields: `releaseCheck` only. The import must report failure and retain the Apple Reminder; the log proves successful journaling and source deletion were refused, not EventKit deletion, sync, or resurrection. No reminder/task identifiers, titles, notes, timestamps, errors, or payloads are logged.

## v1.3.1 (add before tagging, trim in the release after)

- **`v1.3.1/next-action-save-edit`** — desktop `apps/desktop/src/components/TaskItem.tsx` and mobile `apps/mobile/components/project-next-action-prompt.tsx` (including the row fallback), after **Save & edit** has created its follow-up and awaited durable persistence. Message: `Project next action saved for editing`; fields: `releaseCheck`, `stage=persisted`. A failed flush emits no marker; retry persists the existing optimistic task instead of creating another. Confirm that the full task editor opens, the project/section are retained unless explicitly retargeted, and Inbox status or dates can be edited. On iOS the editor must wait until the prompt dismisses. The marker proves persistence before the editor handoff, not native modal presentation. No task content, identifiers, dates, priorities, or raw errors are logged.

- **`v1.3.1/share-handoff-route`** — mobile `apps/mobile/app/+native-intent.ts`, when a recognized share-extension handoff is intercepted before Expo Router treats it as a page. Message: `Share handoff routed`; fields: `releaseCheck`, `stage=handoff-routed`, `delivery` (`cold` or `warm`). Cold starts route to Inbox; warm deliveries suppress Router navigation while the existing share provider opens capture. The marker proves the routing guard ran, not payload delivery or saving: confirm the existing `ios-share-capture` trail reaches `form-mounted` and, after explicit Save, `transaction-returned` with `outcome=success`. Test both launch states and repeated shares without an Unmatched Route screen or duplicate capture. No URLs, shared content, file paths, identifiers, or errors are logged.

- **`v1.3.1/android-system-bars`** — mobile `apps/mobile/lib/android-system-bars.ts`, after the new native method acknowledges a theme application to the activity window. Message: `Android navigation bar style applied`; fields: `releaseCheck`, `backend` (`edge-to-edge` on Android 15+ or `legacy-color` on older Android), `outcome=applied`. Later theme changes must still log if the initial call preceded hydration of the logging preference. The native path uses the insets controller for icon appearance on API 30+ and avoids the legacy navigation color setter on API 35+. A missing activity/controller, optional module, or older native client must not produce this success marker. Exceptions produce `Failed to apply Android system bar colors`, `outcome=failed`, without raw error text. This proves the path ran, not visual contrast; verify light/dark themes and navigation modes on screen. No task content, color, device identifiers, or other private data is logged.

- **`v1.3.1/cloud-assignee-search`** — cloud `apps/cloud/src/server-validation.ts` (#1233), after `/v1/tasks?query=...` or `/v1/search` finishes shared-core matching with a malformed non-string assignee in the searched records. Message: `Cloud search tolerated malformed assignee text`, `outcome=completed`. The operator's server log must show this marker and the response must be HTTP 200; invalid assignee values are ignored for text matching, not changed or removed from storage. New REST task writes reject non-string/non-null assignments with HTTP 400. No task content, query, assignment value, identifier, or credentials are logged.

- **`v1.3.1/apple-pcc-evaluation`** — development-only mobile `apps/mobile/lib/apple-pcc-evaluation.ts` (#1214). Messages: `Apple PCC capability checked`, `Apple PCC evaluation completed`, and `Apple PCC evaluation stopped`. Fields: `releaseCheck`, `backend` (`on_device` or `private_cloud_compute`), `operation` (`capability` or `generation`), allowlisted `outcome`, `durationMs`, and generation-only `fixtureId` (`smoke` or `project_planning`, fixed synthetic fixtures only). A completed `private_cloud_compute` smoke request on a correctly provisioned signed device proves the evaluation request returned a bounded result; a capability line alone does not prove entitlement provisioning, successful inference, model quality, or production availability. Cancellation, timeout, quota/network/service failures, malformed output, and unavailable capability must produce only stable categories and must not trigger automatic retries or fallback. No prompts, responses, generated text, task content, request IDs, raw errors, or credentials are logged. The synthetic harness does not read or mutate task data.

- **`v1.3.1/local-api-project-write`** — desktop `apps/desktop/src-tauri/src/local_api.rs`, after `POST /projects` or `PATCH /projects/:id` commits its project/task/section row transaction and acknowledges the durable write. Message: `Desktop Local API project write persisted`. The tester's log must show this marker after a successful authenticated project create or update; GET readback and an app restart must retain the result. For project archive/reactivation, verify the child task and section lifecycle separately. Validation errors and rolled-back writes emit no marker. No project title, identifiers, area data, request body, token, URL, or other user content is logged.

- **`v1.3.1/local-api-project-lifecycle`** — desktop `apps/desktop/src-tauri/src/local_api.rs` (#1235), after `DELETE /projects/:id` or `POST /projects/:id/restore` commits its project/task/section row transaction. Message: `Desktop Local API project lifecycle completed`; fields: `releaseCheck`, `operation` (`delete` or `restore`), `outcome` (`persisted` or `unchanged`). The tester's log must show `persisted` for the first successful operation and `unchanged` for an idempotent repeat. GET readback and an app restart must retain the result: delete soft-deletes the project and its live sections while detaching live tasks, and restore revives the project plus only matching legacy-cascade children without reattaching detached tasks. Missing or purged projects and rolled-back writes emit no marker. No project or task content, identifiers, request body, token, URL, timestamp, path, or other user content is logged.

- **`v1.3.1/local-api-task-triage`** — desktop `apps/desktop/src-tauri/src/local_api.rs`, after `PATCH /tasks/:id` commits a supported non-terminal status (`inbox`, `next`, `waiting`, `someday`, or `reference`). Message: `Desktop Local API task triage persisted`. The tester's log must show this marker after a successful authenticated triage patch; GET readback and an app restart must retain the status and its normalized lifecycle fields. Rejected terminal/deleted/purged sources, actionable tasks left inside archived projects, terminal targets, and validation failures emit no marker. No task text, identifiers, request body, token, URL, or other user content is logged.

- **`v1.3.1/daily-digest-independent`** — mobile `apps/mobile/lib/notification-service-local.ts`, after enabled morning/evening digest alarms finish reconciling while task reminders are off. Message: `Independent daily digest alarms reconciled`. Fields: `releaseCheck`, `taskRemindersEnabled` (`false`), `morningDigestEnabled`, `eveningDigestEnabled`, `count` (`1` or `2`), `outcome` (`reconciled`). The tester's log must show the selected daily digest boolean and a matching positive count; then its native alarm must remain present across another reconciliation. Disabling one digest cancels only that alarm. This proves the app reconciled the recurring native alarms, not that the OS delivered them; verify foreground and background delivery separately. No task content, identifiers, times, or device data are logged.

- **`v1.3.1/calendar-mirror-filter`** — mobile `apps/mobile/lib/external-calendar.ts`, only when device-calendar events are excluded by a valid push-notes marker or an exact device-local calendar/event mapping. Message: `Mirrored device calendar events excluded`. Fields: `releaseCheck`, `platform`, `stage` (`native-read`), `count` (positive excluded event count). The tester log can show the filter ran while the unprefixed exported task appears once in Mindwtr; it cannot prove that every calendar account has finished syncing or that an older unmapped, unmarked event was found. No task content, event identifiers, calendar names, or account data are logged.

- **`v1.3.1/calendar-date-color-diagnostics`** — mobile `apps/mobile/lib/external-calendar.ts` and desktop `apps/desktop/src/lib/system-calendar.ts`, after a device/system calendar window is read and normalized. Message: `Calendar date and color diagnostic snapshot`; fields: `releaseCheck`, `platform`, `calendarCount`, `eventCount`, `allDayCount`, `timedCount`, `allDayNonMidnightCount`, `localDayAheadCount`, `localDayBehindCount`, `nativeColorCount`, and `fallbackColorCount`. After reproducing a one-day event shift or changing calendar views until a dot changes, the tester's Settings > Diagnostics log must show this snapshot. The aggregate date-boundary counts distinguish timed from all-day conversion and local/UTC day direction; the color counts show whether Mindwtr received a native calendar color or used its deterministic fallback. The marker proves the read path ran, not which individual event or calendar was affected. No event dates or times, titles, notes, locations, calendar names, account details, colors, identifiers, URLs, or credentials are logged.

- **`v1.3.1/someday-section-move`** — desktop `apps/desktop/src/lib/someday-section-move.ts` and mobile `apps/mobile/components/views/use-someday-section-move.ts` (#1220), after a single-task or bulk Someday section assignment or Undo finishes saving. Message: `Someday section assignment saved`. Fields: `releaseCheck`, `count`, `operation` (`move` or `undo`). The tester log must show a positive count after the action; reload must preserve the assignment, task status, project, project section, and dates. A failed save must not emit this success marker. No task or section text or identifiers are logged.

- **`v1.3.1/archive-reactivation-validation`** — `packages/core/src/store-tasks.ts`, after a task mutation reactivates archived project containers and its existing pending save completes. Message: `Archived task container validation saved`. Fields: `releaseCheck`, `outcome` (`reactivated`), `count`. The tester log must show a positive count after reopening an eligible task; its project and strictly archive-owned sections must remain restored after reload. The marker proves successful reactivation through candidate validation, not a measured speedup. No content or identifiers are logged.

- **`v1.3.1/mcp-core-log-stderr`** — `apps/mcp-server/src/index.ts`, once after the first core diagnostic is forwarded by a started MCP server. Message: `Core diagnostic forwarded to MCP stderr`. Fields: `releaseCheck`, `forwardedEventCount` (`1`). The server stderr must contain this marker and retain the original forwarded diagnostic marker, while every stdout line remains JSON-RPC. A successful mutation must remain durable after restart. Importing the library alone does not install the bridge. This proves actual forwarding, not completion of an unrelated core operation. No content or identifiers are added to the proof.

- **`v1.3.1/shortcut-failure-privacy`** — mobile `hooks/root-layout/use-root-layout-external-capture.ts`, when a Shortcut has no readable title or capture navigation fails. Existing invalid-link warnings and `Shortcut capture confirmation failed` carry only `releaseCheck` and `stage` (`invalid-payload` or `navigation`). A diagnostic log must identify the failure stage without the incoming URL, capture fields, or router error text. Navigation failure remains retryable; this marker does not prove a task was saved.

- **`v1.3.1/legacy-capture-audio`** — `apps/cloud/src/server-attachments.ts`, after GET/HEAD reads a still-referenced recording from the exact historical nested capture directory while the canonical file is absent. Message: `Legacy capture audio served from historical layout`. Fields: `releaseCheck`, `method` (`GET` or `HEAD`), `operation` (`legacy-capture-audio-read`), `outcome` (`served`). The server log must show this marker for an old recording and repeat GET/HEAD must return the original bytes/length. This is read-only compatibility: no relocation or metadata migration is claimed. Canonical files take precedence, and stale references, symlinks or lost storage authority cannot authorize fallback. No content, file names, paths, identifiers or credentials are logged.

- **`v1.3.1/capture-token-revocation`** — `apps/cloud/src/server-request.ts`, when an unmapped generated capture credential is refused in any-token mode. Message: `Unmapped capture credential refused in any-token mode`. Fields: `releaseCheck`, `outcome` (`refused`). After revocation and again after restart, the server log must show this marker and requests must return unauthorized without creating a new namespace. The marker also covers unknown generated credentials; it does not prove the credential was previously valid. Active scoped credentials and explicitly allowlisted full credentials keep their authority. No token, digest, content, or namespace identity is logged.

- **`v1.3.1/cloud-attachment-gc-retention`** — `apps/cloud/src/server-attachments.ts`, after orphan cleanup retains referenced attachment files. Message: `Referenced attachment files retained during cleanup`. Fields: `releaseCheck`, `count`, `operation` (`orphan-gc`), `outcome` (`complete` or `partial`). The server log must show a positive count when cleanup encounters referenced files; subsequent GET/HEAD must still read those files while genuine aged orphans are removed. The marker proves retention, not historical-layout readability or a completed sync. No content, identifiers, paths, or credentials are logged.

- **`v1.3.1/archive-section-retention`** — `packages/core/src/sync-tombstones.ts`, when timed cleanup retains expired sections owned by a still-restorable archived project, including a project in its Trash retention window. Message: `Expired archived project sections retained during tombstone cleanup`. Fields: `releaseCheck`, `retainedSectionCount`. The tester's log must show a positive count after loading or syncing an old archived project; reactivating it must preserve its section names, notes, and placement. The marker proves retention, not reactivation or a completed sync. Independently deleted sections still expire. No content or identifiers are logged.

- **`v1.3.1/apple-task-snapshot`** — mobile `lib/widget-service.ts` (#915, #1194), after App Group snapshot publication succeeds. Message: `iOS task snapshot published to App Group`. Fields: `releaseCheck`, `snapshotVersion`, `publishedCount`, `omittedCount`, `exactLinkCount`. The tester's log must show version 2 and exact-link counts for published task identities, including a positive omitted count when the bounded snapshot excludes eligible tasks. This proves the snapshot was published, not that Core Spotlight finished indexing or that a Siri mutation completed. No task text, links, or identifiers are logged.

- **`v1.3.1/apple-inbox-clarification`** — mobile `lib/apple-foundation-models.ts` and `components/inbox-processing/useInboxProcessingController.ts` (#1214). Fields: `releaseCheck`, `backend` (`apple_on_device`), `outcome`, `reason`, `statusIncluded`, `associationCount`, `dateCount`, `failureClass`. In an iOS development build with on-device clarification selected, the log must show `suggestion_ready` after validated generation and `applied_to_draft` only after an explicit Apply to the unchanged draft. `stale_ignored` means a newer edit prevented application; `unavailable` and `failed` identify paths that produced no suggestion. Draft application is not a durable task save; the normal processing Save still owns persistence. No task text, prompts, responses, candidate identifiers, or credentials are logged.

- **`v1.3.1/apple-search-evaluation`** — mobile `lib/apple-search-evaluation.ts` (#1194). Fields: `releaseCheck`, `stage`, `matchCount`, `acceptedCount`, `droppedCount`, `elapsedMs`. Run a query in the development evaluation screen: `completed` proves native results were returned and checked against explicit filters and available task identities; `cancelled` or `failed` proves no completed result was returned. Counts describe this bounded result set, not the full store or model quality. No query, result text, or task identifiers are logged.

- **`v1.3.1/apple-image-capture-evaluation`** — mobile `components/AppleImageCaptureEvaluation.tsx` (#1195), called by the image evaluation controller. Fields: `releaseCheck`, `stage`, `outcome`, `kind`. `analysis/completed/comparison` proves the bounded native comparison returned; it does not prove that the model produced a useful task. After explicit Save, `save/persisted/task` must appear only once the core-created task's pending save completes. Cancel must create no task, and a failed save must show `failed` rather than `persisted`. No image, file path, extracted text, prompt, response, or task identifier is logged.

- **`v1.3.1/file-sync-attachment-failure`** — desktop `apps/desktop/src/lib/file-sync-attachment-diagnostics.ts` and `sync-attachment-backends.ts`, when a File Sync attachment upload operation fails. Message: `File Sync attachment operation failed`. Fields: `releaseCheck`, `backend` (`file`), `operation` (`upload`), `stage`, `errorType`, `nativeCode`, and from v1.3.2 `reason`. `reason` is the native refusal sentence, logged ONLY when it exactly equals one of the fixed sentences in `src-tauri/src/file_sync_attachment_publication.rs` or the lease wrapper (for example `Attachment publication journal is full`), otherwise `unlisted`; a reporter's 1.3.1 log (2026-09-21, Windows 10, Microsoft Store) showed `stage: generation-reserve`, `errorType: string`, `nativeCode: unknown`, which names the step but none of its forty rules. Enable Diagnostics, retry the failing upload on Windows, and share the fresh log: the marker must identify the failed source-read or generation-publication stage, with a safe error category and native error code when available (`unknown` otherwise). This is diagnostic coverage, not an upload fix or proof that sync completed. The original transfer error, cleanup, retries, and pending-upload safety gate remain unchanged. No paths, file names, content, identifiers, or stacks are logged by this marker, and no error message other than an allow-listed fixed sentence.

- **`v1.3.1/ios-widget-parity`** — `apps/mobile/lib/widget-service.ts` after all five family snapshots publish, and `apps/mobile/lib/ios-widget-completions.ts` after one or more durable widget completions are acknowledged. Messages: `iOS widget parity snapshot published` (`count`, `totalItems`, `nextItems`) and `iOS widget parity completions ingested` (`count`). With Diagnostics enabled, publish a snapshot containing Next Actions, complete several visible Today/Focus rows, wait through the Undo grace, then open Mindwtr: positive count-only markers must accompany the cached refill and single-writer ingestion. These lines prove JavaScript publication/ingestion, not exact WidgetKit timing or visible rendering; verify the widget separately. No task text, identifiers, tokens, routes, or timestamps are logged.

- **`v1.3.1/local-crash-capture`** — mobile `lib/mobile-crash-capture.ts` and `lib/app-log.ts`, after startup successfully imports the newest retained fatal JavaScript record into the normal serialized Diagnostics log. Message: `Retained fatal JavaScript crash recovered`. Fields: `releaseCheck`, `count` (`1`, the only quantity). The adjacent imported error line carries the prior crash timestamp, a conservative recognized technical message shape (or a fixed privacy-omission message), bounded technical JavaScript frames, and fixed metadata `platform`, `appVersion`, `buildVersion`, and `exceptionType`; the retained slot itself never carries the recovery marker. Reproduce the reported JavaScript crash, reopen Mindwtr, and share Settings > Diagnostics: the log must show the imported error followed by this marker. The marker proves the JavaScript fatal handler synchronously completed a best-effort app-private slot write before delegating and that the next startup imported that slot; merely installing the handler or retaining an as-yet-unimported record does not emit it. It does not identify or fix the original crash, prove power-loss durability, or cover native crashes, OS kills, failures before handler installation, an unavailable file-system/JavaScript runtime, or termination during the synchronous native write. Arbitrary exception text, the first raw stack line, task data, URLs, credentials, paths, object dumps, identifiers, and breadcrumbs are not retained; only recognized technical shapes and normalized function/file/line/column frames are eligible. The slot contains one record of at most 16 KiB, is not synced or uploaded automatically, and is also removed by Clear log.

- **`v1.3.1/ios-scene-lifecycle`** — mobile `hooks/use-ios-scene-diagnostics.ts`, draining the native scene lifecycle's bounded in-memory diagnostic buffer. Message: `iOS scene lifecycle event`. Fields: `releaseCheck`, `stage`, `deliveryKind`, `count`. With Diagnostics enabled, cold-launch through a Shortcut, repeat it while warm, and background/foreground the app: the log must show scene connection/root startup and the matching cold/warm delivery stages. Each deliberate invocation remains distinct. This proves native lifecycle routing reached the JavaScript diagnostic bridge, not capture persistence, authentication, or successful navigation. Verify saved tasks separately. No task text, URLs, identifiers, or persisted native diagnostic data are recorded.

- **`v1.3.1/widget-checkoff-hide`** — Android `modules/android-widget` records a content-free count after a durably queued completion requested a partial row/header/count/empty update. Android 12+ uses direct `RemoteCollectionItems`; older Android uses legacy collection invalidation plus partial chrome. `apps/mobile/lib/widget-service.ts` writes it to Diagnostics on the next app-owned widget publication. Message: `Android widget check-offs hidden after Undo`. Fields: `releaseCheck`, `count` (completed rows covered by successful native partial-refresh requests). Check a visible Tasks row (or use Compact's task detail sheet), leave the app closed beyond Undo, then open Mindwtr: the log must show a positive count. Separately confirm the row disappeared after Undo, another row filled the space, later widget taps still work, and opening the app applied the completion. The marker proves the native refresh request ran; it cannot prove what the launcher rendered. Queue-write or partial-refresh failures do not emit it and remain retryable. No task content, identifiers, queue names, or widget identifiers are logged. (#1211)

- **`v1.3.1/pending-capture-widget-refresh`** — `apps/mobile/hooks/root-layout/use-root-layout-pending-captures.ts` (#1211). Message: `Widgets refreshed after pending capture import`. Fields: `releaseCheck`, `count` (imported queue item count). Capture from the widget, then open Mindwtr once: the log must show a positive count after durable import and successful widget snapshot publication, including when import takes longer than the foreground refresh timer. The Inbox widget must update without a manual app refresh. Closed-app captures remain queued until the app runs; this marker does not prove launcher rendering. No task content or identifiers are logged.

- **`v1.3.1/android-widget-lists`** — `apps/mobile/lib/widget-service.ts` (#1211). Message: `Android widget fixed lists published`. Fields: `releaseCheck`, `count` (`5`), `focusItems`, `nextItems`, `inboxItems` (bounded row counts). After opening the app once with logging enabled, the snapshot must include standard lists even when no Tasks widget is placed. With no Focus/Today rows and a positive Next count, Compact must show Next Actions under that title; starring a task restores Focus-first display. A newly placed Tasks widget must show its cached Inbox rows without another app opening. The marker proves snapshot publication, not launcher rendering; confirm the visible widget too. No task text or identifiers are logged.

## v1.3.0 (add before tagging, trim in the release after)

- **`v1.3.0/project-lifecycle-sync`** — `packages/core/src/sync-normalization.ts`, when sync canonicalization clears an archived project's Focus flag or repairs cancellation/status coherence using the same rule as load. Message: `Project lifecycle normalized for sync`. Fields: `releaseCheck`, `count` (`1`). A tester syncing a legacy affected project must see this marker, then no repeated project conflict after the canonical document is saved on both sides. The marker proves normalization, not persistence or a successful full sync. No project text, identifiers, or timestamps are logged.

- **`v1.3.0/feedback-diagnostics`** — mobile and desktop `lib/app-log.ts`, in the opt-in `Feedback diagnostics snapshot`. Fields: `releaseCheck`, `captureMode` (`recent-session-and-saved-log`), `debugLoggingEnabled`, and the existing breadcrumb fields. With debug logging off, reproduce a warning/error, then include diagnostics in feedback within 30 minutes without restarting. The attachment must retain the sanitized event and this marker as complete JSON lines. Routine traffic must not displace errors; Clear log also clears the volatile buffer. The buffer retains at most 160 entries / 64,000 characters in memory and does not enable persistent logging or automatic uploads. This proves collection, not that the app recorded the reported operation or that feedback delivery succeeded.

  For the iOS file-picker report, `apps/mobile/lib/storage-file.ts` records folder picker requested, file fallback requested/declined, and file picker requested/returned (`outcome`: `cancelled` or `selected`). These content-free events locate the last reached step; `requested` does not prove that iOS displayed the picker. Reproduce and submit on the affected device. Failures before the app starts, uninstrumented operations, or failures on another device are still outside this capture.

### Added


- **`v1.3.0/dropbox-oauth-route`** — mobile `app/+native-intent.ts`, when Expo Router recognizes the exact Dropbox native callback and routes its own navigation to Sync settings. Message: `Dropbox OAuth callback routed`. Fields: `releaseCheck`, `stage` (`callback-routed`). This forced app-log marker proves the query-free routing decision only; it does not prove that Settings rendered, AuthSession accepted the original callback, OAuth succeeded, credentials were saved, or sync completed. No callback URL, query, fragment, authorization code, state, token, error text, or user content is logged. (#1207)

- **`v1.3.0/checklist-insert-focus`** — mobile `components/task-edit/TaskEditContentField.tsx`, when Android requests focus for a newly inserted checklist row on the animation frame after its native layout. Message: `Checklist insertion focus requested after layout`. Fields: `releaseCheck`, `stage` (`layout-ready`). With Diagnostics enabled, cold-start the app, open a task's checklist, and tap **Add Item** or insert a row with Enter: the log must show the marker once per accepted focus request. Cancelled, removed, or superseded requests must not emit it. This proves the deferred focus path ran; separately confirm that the keyboard opens and the new row stays visible above it. No task text, list text, identifiers, or screen contents are logged.

- **`v1.3.0/automatic-background-sync`** — mobile `lib/background-sync-task.ts`, after foreground reconciliation establishes the automatic background schedule. Message: `Automatic mobile background sync schedule ready`. Fields: `releaseCheck`, `interval` (`15m`), `outcome` (`registered` or `unchanged`). With Diagnostics enabled, open a configured supported backend after upgrading from any previous interval, including Off: the log must show the automatic schedule ready after successful registration or confirmation of an existing matching registration. Deferred headless reconciliation and failed registration must not emit this marker. It proves registration policy, not that the OS ran a job or that a sync succeeded. No configuration values, credentials, task content, or identifiers are logged.

- **`v1.3.0/ios-share-capture`** — mobile `lib/share-intent-diagnostics.ts`, `hooks/root-layout/use-root-layout-external-capture.ts`, and `app/capture-modal.tsx`, for the iOS host-side incoming-share path. Message: `iOS incoming share diagnostic`. Every line has `releaseCheck` and a fixed `stage`: `provider-status`, `host-url-received`, `payload-seen`, `waiting`, `processing-started`, `files-prepared`, `payload-missing`, `native-error`, `navigation-requested`, `navigation-returned`, `reset-requested`, `reset-returned`, `form-mounted`, `submit-started`, `submit-rejected`, `transaction-returned`, `bulk-confirmed`, or `cancel`. Depending on the stage, safe fields are `type` (`text` | `weburl` | `file` | `media` | `unknown`, or `single` | `bulk` in the form), `providerReady`, `dataReady`, `disabled`, `fileCount`, `candidateCount`, `attachedCount`, `skippedCount`, `count`, and `outcome` (a fixed readiness, failure, or success classification). Reproduce Safari sharing on a cold and warm launch, then share the Settings > Diagnostics log: the sequence separates app URL receipt, provider/data readiness, payload processing, navigation request/return, capture-screen React mount, and the existing capture transaction's returned result. These forced app-log lines do not require debug logging and contain no shared text, subject, URL, file name/path, identifiers, or raw error. The trace cannot prove that the share extension started or diagnose a failure before it launched the host app; `form-mounted` narrows the failure to route/component mounting but is still not native visual confirmation; `transaction-returned` reports the existing transaction promise's success and adds no independent durability verification. No Safari fix is claimed.

- **`v1.3.0/android-saf-backup-write`** — mobile `lib/data-transfer.ts`, on the existing `Backup export complete` log after an Android Storage Access Framework document write returns successfully. Message: `Backup export complete`. Fields: `releaseCheck` plus the existing safe export counts. Export a backup to an Android document provider with Diagnostics enabled: the exported log must show this marker after the provider write returns. The share fallback must not emit it. This proves the document write returned, not an independent durable readback from the provider. No task content, document URI, file name, or provider details are logged.

- **`v1.3.0/derived-token-timestamps`** — desktop `hooks/useListViewOptimizations.ts`, once per WebView session after a list commits with nonempty context or tag statistics from the shared core derivation. Message: `List token statistics available from shared timestamp derivation`. Field: `releaseCheck`. With logging enabled at launch, open a task list containing contexts or tags: the exported log must show this marker. It proves the updated derivation is available to the list, not how many repeated parses were avoided, a measured latency improvement, or durable persistence. No task content, token names, identifiers, dates, or settings values are logged.

- **`v1.3.0/capture-failure-announcement`** — mobile `app/capture-modal.tsx`, when Android requests the explicit accessibility announcement after a failed capture has rendered and the submission has settled. Message: `Capture failure accessibility announcement requested`. Fields: `releaseCheck`. A tester whose failed Save was previously silent must see this marker once per failed attempt, then separately confirm that TalkBack speaks the localized `task.addFailed` message. A new submission or unmount before the scheduled request emits no marker. The marker proves the React Native announcement API was requested, not that TalkBack spoke it. No task content, identifiers, error details, or navigation data is logged.


- **`v1.3.0/widget-batch-derivation`** — mobile `lib/widget-service.ts`, after all five iOS family payload writes complete through one invocation-local derivation. Message: `iOS widget family payloads published from one derivation`; fields `releaseCheck`, `count` (`5`). With Diagnostics enabled, change a widget-visible task: the exported log must show this marker after successful family writes. Failed publication must not emit it and must remain retryable. This proves JavaScript publication, not WidgetKit rendering or an iPhone latency improvement. No task content, identifiers, storage keys or file paths are logged.

- **`v1.3.0/native-command-save-recovery`** — mobile `lib/pending-capture-persistence.ts`, after a replayed native completion/defer command detects exhausted core saves and successfully retries persistence. Message: `Native command save recovered`. Fields: `releaseCheck`, `outcome` (`recovered`). After a failed save, a tester retries ingestion: the marker must precede native acknowledgement, and the completed/deferred task (including one recurring follow-up) must survive reload. Failed recovery retains the command and must not emit this marker. No task text, identifiers, or file paths are logged.

- **`v1.3.0/share-card-export`** — desktop and mobile `lib/share-card-export.ts`, after a local PNG is created for sharing, saving, or copying. With Diagnostics enabled, create a card from Weekly Review: the exported log must show the marker at the verified PNG/export stage. Failed mobile exports log `failureStage`, `errorType`, and an optional native `nativeCode` without the platform error message. This proves the local export path ran, not that a recipient received the image or that the QR destination is reachable. No task or project names, identifiers, reflection text, image contents, error messages, or destination file paths are logged.

- **`v1.3.0/task-editor-upgrade-sync`** — `packages/core/src/sync-merge-settings.ts`, when layout conflict resolution accepts an incoming layout over migration-generated defaults or resolves a tied/missing GTD clock. Message: `Task editor layout sync conflict resolved`. Field: `reason` (`explicit-layout` or `tied-clock`). With GTD settings sync enabled, sync existing clients whose layouts differ without editing a layout option: the receiving client must show the marker, both layouts must agree after syncing, and aligned syncs must stop emitting it. This proves merge selection, not durable upload; verify sync completion separately. No layout values, task text, identifiers, or timestamps are logged.

- **`v1.3.0/android-adaptive-window`** — mobile `hooks/use-adaptive-window.tsx`, after native window information commits a changed layout/posture. Message: `Android adaptive window updated`. Fields: `releaseCheck`, `count` (active separating/occluding folding features), `reason` (compact/expanded and flat/book/tabletop). Resize or fold the Android app with Diagnostics enabled: the exported log must show the resulting layout/posture, without repeated entries for unchanged geometry. This proves the native adaptation path ran, not draft restoration or physical Galaxy compatibility. Runtimes without the native module must not emit this success marker. No task text, identifiers, device model, or screen contents are logged. (#1192)

- **`v1.3.0/android-activity-session-recovery`** — mobile `app/_layout.tsx`, after a verified configuration replacement restores the sanitized React Navigation root through `resetRoot`, and `components/task-edit-modal.tsx`, after Android confirms a recovered task input accepted focus. Messages: `Android activity navigation restored` and `Android activity editor input restored`. Fields: `releaseCheck`, `outcome` (`restored` or `focused`), `surface` (`navigation`, `title`, or `description`). With Diagnostics enabled, leave a regular task or project flow open and trigger Activity recreation: the exported log must show the navigation marker once the replacement navigation container is ready; an editor whose title or description had focus must also show the matching focused-input marker. These prove the same-process recovery paths ran; they do not cover process death. No task text, identifiers, route parameters, device model, or screen contents are logged. (#1192)

- **`v1.3.0/sandbox-workspace`** — desktop `lib/sandbox-session.ts` and mobile `lib/sandbox-workspace.ts` emit `stage=entry-idle` after draining personal sync and local writes while new foreground sync admission is blocked. Opening must stay in the personal workspace if that check fails or times out. Desktop `main.tsx` and mobile startup then emit `workspace=sandbox` after selecting isolated in-memory storage. The tester log must show the entry marker followed by the workspace marker when opening; resetting only needs the workspace marker. Verify the visible banner, editing/reset/exit, and unchanged personal data separately. No task text, personal settings, file paths, or credentials are logged. (#1196)

- **`v1.3.0/macos-swift-runtime`** — `apps/desktop/src-tauri/src/lib.rs`, at the end of macOS native setup after registering diagnostics logging. Message: `macOS native startup reached after runtime loading`. On the affected Intel Mac, enable Diagnostics and relaunch the next build; the log must contain this marker. It proves dyld loaded the native app and setup reached this point, not that the UI or every optional native capability works. No device identifiers or task data are logged. Native CI also loads the release library, and macOS release packaging checks the executable runtime search path for both architectures.

- **`v1.3.0/shortcut-capture-single-owner`** — `apps/mobile/hooks/root-layout/use-root-layout-external-capture.ts`, after the validated shortcut delivery requests its prefilled confirmation. Message: `Shortcut capture confirmation requested`. With Diagnostics enabled, run an Action Button capture on both a cold and warm iPhone launch: this marker must appear once per delivery, the title and optional fields must be preserved, and Save with Add another off or Cancel must leave no blank capture form. A later deliberate invocation must open again. The marker proves the confirmation navigation request, not a completed save or on-device visual confirmation. No task content, URL, or identifiers are logged.

- **`v1.3.0/dropbox-callback-stream`** — desktop `sync.rs`, after an accepted Dropbox OAuth callback socket is explicitly configured for blocking IO with bounded read and write timeouts. Message: `Dropbox OAuth callback stream configured`. The tester log must show this marker during Dropbox sign-in, followed by the existing matching-state acceptance line when authorization succeeds; a stale callback must still be rejected while the valid callback remains acceptable. This marker proves socket configuration, not successful authentication. No authorization code, state, token, query, or credential content is logged.

- **`v1.3.0/calendar-push-inventory`** — desktop `desktop-calendar-push-sync.ts` and mobile `calendar-push-sync.ts`, on the existing full calendar push completion log after the shared run returns. A full run must show this marker with its existing task and failure counts. It proves the run used an invocation mapping inventory instead of per-task storage lookups; it does not claim every event or mapping write succeeded. Partial runs do not emit this marker. No task content, calendar account data, or credentials are logged.

- **`v1.3.0/watch-outbox-retry`** — `apps/mobile/lib/pending-captures.ts`, after a Watch text or audio capture marked as retried from a failed outbox save reaches task persistence, pending saves finish, and its queue item is removed. Message: `Watch outbox retry ingested`. Fields: `releaseCheck`, `kind` (`text` or `audio`), `outcome` (`created`). On Watch, a failed text outbox save must disable the separate Type control while Edit capture and Resend capture remain available. Recover using Resend capture or Retry recording; the phone log must show this marker once delivery and ingestion succeed. A failed phone flush must emit no success marker. Retry ownership before the Watch outbox save succeeds is in-memory; this marker does not claim survival of Watch process termination during that failure. No task content or identifiers are logged.

- **`v1.3.0/archive-section-preserved`** — desktop `storage.rs`, when the native reference sanitizer preserves valid section links owned by an archived project. Message: `Archived project section references preserved`, with `extra.releaseCheck` and `count`. Archive or cancel a project containing sectioned tasks, save and restart, then reactivate it: the native log must show this marker, valid links must survive, and unedited actionable children must restore. The marker proves the relationship check ran, not that the surrounding storage transaction committed. No task content or identifiers are logged.

- **`v1.3.0/archive-reference-preserved`** — `packages/core/src/store-load-migrations.ts`, when loading a normally archived project preserves its reference children. Message: `Archived project reference preserved during load migration`. Fields: `releaseCheck`, `count`. Archive a project containing reference material, then restart: the log must show the preserved count and the reference task must keep its status and completion metadata. This marker proves the preservation check ran; it does not report a new write. No task content or identifiers are logged.

- **`v1.3.0/mobile-cancel-draft`** — `apps/mobile/components/task-edit/use-task-edit-state.ts`, after Cancel writes the validated task draft as one archived outcome and pending storage saves finish. Message: `Mobile task cancellation draft saved`. Fields: `releaseCheck`, `outcome` (`cancelled`). Edit the title or Notes, add a file, and cancel the task: the log must show this marker after the save succeeds, the editor must close, and the archived task must retain the edits without creating another recurrence. A rejected write or failed flush must keep the editor open and emit no success marker. No task content or identifiers are logged.

- **`v1.3.0/widget-terminal-preserved`** — `apps/mobile/lib/pending-captures.ts`, after an Android widget completion targeting an archived or cancelled task is resolved without changing the task, pending saves finish, and the queue file is removed. Message: `Widget check-off ingested`. Fields: `releaseCheck`, `outcome` (`terminal`). Queue a widget check-off, cancel or archive that task before ingestion, then open the app: the log must show this marker and the task must retain its archived outcome without generating another recurrence. No task content or identifiers are logged.

- **`v1.3.0/watcher-property-order`** — desktop `local-data-watcher.ts`, once per controller after preparing and retaining a self-write snapshot with the shared serializer's bounded property-order reuse. Message: `Self-write snapshot prepared with bounded property ordering`. Field: `releaseCheck`. With logging enabled at launch, a local save must emit this line; an earlier save may emit it first. It proves snapshot preparation ran, not that a particular shape hit the cache, persistence completed, or latency improved. No task content, identifiers, property names, or settings values are logged.

- **`v1.3.0/storage-baseline-equality`** — desktop `storage-save-baseline.ts`, once per WebView session when two distinct snapshots compare structurally equal and skip fingerprint construction. Message: `Storage snapshot comparison skipped fingerprinting`. Field: `releaseCheck`. With logging enabled at launch, a save comparing cloned unchanged rows must emit this line; an earlier baseline/provenance/settings comparison can emit it first. It proves the positive-equality shortcut ran, not a durable commit or a measured latency. Structural mismatches still use the existing stable fingerprint path. No entity content, identifiers, or settings values are logged.

- **`v1.3.0/sqlite-snapshot-append`** — desktop Rust `storage.rs`, once per process after a normal snapshot merge commits with the append-only task path. Message: `SQLite capture snapshot retained unchanged tasks`. Fields: `releaseCheck`, `count` (retained task rows). Capture a task into an existing dataset with no simultaneous changes: a positive count proves existing task rows were retained and the transaction committed. Edits, container/settings changes, reference repairs affecting existing rows, and exact restores keep the full replacement path. This does not prove recovery JSON publication or a specific latency. No task content, IDs, paths, or credentials are logged.

- **`v1.3.0/sqlite-snapshot-statements`** — desktop Rust `storage.rs`, once per process after `persist_data_snapshot` commits using connection-cached INSERT statements. Message: `SQLite snapshot committed with cached inserts`. Fields: `releaseCheck`, `count` (canonical task count). A tester capturing or saving a full snapshot must see the line in the exported Diagnostics log; an earlier normal save may emit it first. It proves the cached-statement path committed, not completion of the recovery JSON publication or a measured latency improvement. SQL, foreign-key/FTS behavior, revision arbitration, and transaction boundaries remain unchanged. No task content, identifiers, paths, or credentials are logged.


- **`v1.3.0/ai-request-stop-once`** — desktop/mobile `lib/ai-config.ts`, called by the AI provider when a request terminates on timeout or cancellation without retrying. Message: `AI request stopped without retry`. Fields: `releaseCheck`, `outcome` (`timeout` | `aborted`), `provider`, `timeoutMs` (configured limit). With Diagnostics enabled, choose 120 seconds under AI → Advanced and time out one request: the log must show one `timeout` entry with `timeoutMs=120000`, not three attempts. Cancelling must show `aborted` and dispatch no further attempt. Applies to assistant/Copilot requests, not transcription or model discovery. No task text, prompts, API credentials, endpoint, or model name is logged. (#1188)

- **`v1.3.0/sync-attachment-copy-elision`** — core `sync.ts`, after full merging and reference repair, when at least one matched task/project kept its normalized winner because the attachment result was unchanged. Message: `Sync merge skipped unchanged attachment copies`. Fields: `releaseCheck`, `count` (avoided parent copies). With Diagnostics enabled, merge overlapping tasks without attachments and confirm a positive count. This proves the allocation-saving branch ran, not durable persistence or remote acknowledgement. Attachment reconciliation, conflict arbitration, cancellation and recurrence repairs still execute. No task content, identifiers, paths or credentials are logged.

- **`v1.3.0/settings-default-coload`** — desktop `SettingsView.tsx`, in the General page layout effect. Message: `[settings-open] settings-view-layout-effect`, with `page=main` and the existing trace timing fields. With Diagnostics enabled, first opening Settings must show this line with General content available in the route's initial commit, without a second page-code loading fallback. This proves a committed default page, not completion of asynchronous native configuration reads. Other sections retain `settings-page-transition`; no settings values, task content, or paths are logged.

- **`v1.3.0/settings-page-transition`** — desktop `SettingsView.tsx`, in the page layout effect after a settings destination commits. Message: `[settings-open] settings-view-layout-effect`. Fields: `releaseCheck`, `page` (fixed settings page name), plus the existing trace timing fields. With Diagnostics enabled, navigate from General to a previously unopened section: the old page should remain visible while the code loads, followed by this line for the destination. This proves a page commit, not completion of its asynchronous configuration reads. No task content, settings values, or paths are logged.

- **`v1.3.0/pomodoro-alert-delivery`** — mobile `lib/notification-service-local.ts`, when normal timer completion reaches JavaScript at or after the saved deadline and leaves the native completion alarm intact for OS delivery. Message: `Pomodoro due alarm preserved`. Fields: `releaseCheck`, `reason` (`timer-not-running` or `phase-replaced`), `outcome` (`preserved`), `count` (`1`). With Diagnostics enabled, a tester completing a session while Android delays an inexact alarm must see this line before the later audible notification. It proves Mindwtr did not cancel the due native alarm; it does not claim that the OS delivered or presented it. No task text, alarm id, timestamp, or notification content is logged.

- **`v1.3.0/sync-signature-pruning`** — core `sync.ts`, after the normal sync cycle completes full snapshot merging using signatures that filter absent/excluded fields before sorting. Message: `Full sync merge completed`. Fields: `releaseCheck`, `elapsedMs` (elapsed merge time only), `count` (merged tasks). With Diagnostics enabled, a tester syncing a real remote snapshot must see the line; the explicit empty-remote bypass must not emit it. This proves merge computation completed, not local persistence or remote acknowledgement. No task text, identifiers, paths, or credentials are logged.

- **`v1.3.0/settings-lazy-resources`** — desktop `SettingsView.tsx`, `[settings-open] settings-resource-activation`. With Diagnostics enabled, a fresh General Settings visit must show `page=main` and `integrationsLoadEnabled`, `syncLoadEnabled`, `advancedLoadEnabled` all false. Visiting Integrations, Sync/Data, or Advanced enables only that resource group; it stays enabled until Settings closes so drafts survive navigation. Credential recovery remains active. No configuration values or task content are logged.

- **`v1.3.0/startup-readiness`** — mobile `hooks/use-startup-screen-ready.tsx` and desktop `lib/startup-profiler.ts`. Message: `Startup screen ready`, with `elapsedMs` and (mobile) a fixed `route`; desktop additionally records `moduleElapsedMs`. With Diagnostics logging enabled, the line must appear only after successful canonical local hydration and the active screen's paint opportunity, never for a backup-only/error/locked screen. Mobile detailed profiling also emits `js.interactive_ready`; Android's optional native module calls `reportFullyDrawn()`. These app-side milestones are not a measurement of input acknowledgement or total native process launch. No task content is logged.

- **`v1.3.0/macos-widget-capture`** — `apps/desktop/src-tauri/src/macos_widget.rs`, when the widget's payload-free native notification reaches the host. Message: `macOS widget capture request delivered to host`, carrying `extra.releaseCheck=v1.3.0/macos-widget-capture`. A tester tapping Quick capture on the macOS widget must see this line and only the floating Quick Add panel, with no main-window reveal. Verify both a fully quit and an already-running app; Save must create one Inbox task through the normal store and Cancel must create none. This line proves request delivery, not a completed task save. No task text or identifiers are logged.
- **`v1.3.0/android-compact-widget`** — `apps/mobile/lib/widget-service.ts`, after the native refresh reports a placed Compact widget. Message: `Compact Android widgets refreshed`. Fields: `releaseCheck`, `count` (Compact widgets asked to redraw). A tester must see a positive count after opening the app with this style placed. Confirm the small flat list visually and capture from its footer while the main app is closed; the existing `Quick capture dialog item ingested` line proves the queued task reaches the store on the next app foreground. No task content or widget identifier is logged.

- **`v1.3.0/file-sync-streaming-permissions`** — `apps/desktop/src/lib/sync-attachment-backends.ts`, after a File Sync attachment generation is published or verified for reuse, or a downloaded attachment is installed. Message: `File Sync attachment transfer completed`. Fields: `releaseCheck`, `operation` (`upload` | `download`). A tester adding a file and syncing must see `operation=upload`; a peer receiving it must see `operation=download`. The line proves attachment transfer or verified reuse, not completion of the whole sync cycle. No file name, path, content, or identifier is logged. Native streaming-command refusals retain only the fixed command name in the warning.
- **`v1.3.0/reopen-project-task`** — `packages/core/src/store-tasks.ts` and `apps/cloud/src/server.ts`, only after the task-triggered project reactivation task/project/section snapshot has reached durable storage. Messages: `Task project reactivation saved` (app store) and `Cloud task reopened archived project` (Cloud REST). Fields: `releaseCheck`, `outcome` (`reactivated`), `count` (reactivated projects). A failed write, including an unchanged retry of an earlier optimistic state, emits no reactivation success line. No task content, entity id, path, namespace, token, credential, or timestamp is logged.
- **`v1.3.0/commitment-cancelled`** — `packages/core/src/store-tasks.ts` and `packages/core/src/store-projects/project-actions.ts`, after `cancelTask` or `cancelProject` has flushed the accepted cancellation to durable storage. Message: `Commitment cancellation saved`. Fields: `releaseCheck`, `kind` (`task` | `project`), `outcome` (`cancelled`), `count` (`1`). A tester cancelling a task or project must see exactly one success line after the write persists; a storage failure returns an action error and emits no success line. No title, notes, entity id, timestamp, attachment data, or cancellation reason is logged.
- **`v1.3.0/widget-focus-today`** — `apps/mobile/lib/widget-service.ts`, after Android accepts the curated widget payload. Message: `Android widget Focus and Today payload published`. Fields: `releaseCheck`, `focusItems`, `todayItems`, `totalItems`. The tester's log must show the published counts for Today's Focus and Today, within the shared 20-task Android limit. Other Focus sections are omitted from the default widget; explicit chooser lists remain available. No task content or identifiers are logged. (#1173)
- **`v1.3.0/ios-focus-widget`** — `apps/mobile/lib/widget-service.ts`, after all iOS family payloads are written and timelines requested. Message: `iOS Focus widget payload published`. Fields: `releaseCheck`, `focusItems`, `todayItems`, `totalItems` (default-family refill cache, at most 20; rendered rows remain geometry-capped). The tester must see this line after changing Focus filters or tasks, then visually confirm Today's Focus and Today, selected lists, and the appropriate widget size. Publication does not prove WidgetKit rendered the timeline. No task content, identifiers, tokens, routes, or timestamps are logged. (#1173)
- **`v1.3.0/ios-widget-checkoff`** — `apps/mobile/lib/ios-widget-completions.ts`, after a claimed App Group completion is resolved through the normal store, pending saves flush, a fresh widget payload is published, and the action is acknowledged. Message: `iOS widget completion ingested`. Fields: `releaseCheck`, `outcome` (`completed`, `already-done`, `terminal`, `missing`, or `stale`). Check off from an iOS 17+ Home Screen widget, then open Mindwtr: the line must appear and the task must be done exactly once, including recurrence. Undo before the app claims an action must produce no completion. A changed or reopened task must produce `stale` without changing it. A failed save must retain the action for retry. No task content or identifiers are logged. (#1173)
- **`v1.3.0/cloud-focus-write-parity`** — `apps/cloud/src/server.ts`, after a REST task creation requests Focus or a normalized PATCH changes Focus from false to true. Message: `Cloud task Focus write policy applied`. Fields: `releaseCheck`, `operation` (`create` | `patch`), `outcome` (`focused` | `refused-ineligible` | `refused-limit`), `count` (active Focus tasks after an accepted write, or at refusal). A refused create still returns the captured task with its requested status and no star; a refused PATCH returns `Focus limit of <limit> reached`. No task content, id, namespace, or token is logged.
- **`v1.3.0/webdav-presence-proof`** — `apps/desktop/src/lib/sync-attachment-backends.ts` and `apps/mobile/lib/attachment-sync-backends/webdav.ts`, after the shared remote attachment presence proof finishes. Message: `WebDAV attachment presence proof finished`. Fields: `releaseCheck`, `checked`, `cleared`, `complete`. A tester recovering from a temporary WebDAV 401, 500, or network failure must first see `complete=false`, then a later recovered cycle with `complete=true`; only the complete cycle may suppress another proof for 24 hours. `cleared` counts definitive missing objects queued for same-cycle repair. No URL, credential, path, title, task text, or attachment identifier is logged.
- **`v1.3.0/watch-audio-ready`** — `apps/mobile/lib/pending-captures.ts`, after the JS queue drain accepts a Watch audio path and before transcription starts. Message: `Watch audio ready for transcription`. Fields: `releaseCheck`, `outcome` (`validated`). Reaching this line proves the native receiver published a validated WAV and its queue item; a tester's successful recording must show it followed by the existing `Watch capture ingested` line, while a later transcription failure leaves the WAV and queue item for retry. No recording content, path, or identifier is logged. (#1175)
- **`v1.3.0/watch-capture`** — `apps/mobile/lib/pending-captures.ts`, after a Watch text or transcribed audio capture reaches durable task storage and leaves the pending queue. Fields: `releaseCheck`, `kind`, `outcome`. The tester must see `Watch capture ingested` with `outcome=created` for each ingested Watch capture. Audio transcription failure/unavailability logs that outcome and retains the queue and recording for retry. No title, transcript, audio path, or task identifier is logged. (#1175)
- **`v1.3.0/watch-command`** — `apps/mobile/lib/pending-captures.ts`, after a Watch completion, defer, or Pomodoro command is applied or resolved as a no-op and removed from the queue. Message: `Watch command ingested`. Fields: `releaseCheck`, `kind`, `action` (Pomodoro only), `outcome`. A tester's log distinguishes an applied command from a stale or terminal-task no-op after reconnecting. (#1175)
- **`v1.3.0/watch-numeric-bridge`** — `apps/mobile/hooks/root-layout/use-root-layout-watch.ts`, after the awaited native bridge accepts the Focus/timer application context. Message: `Watch snapshot published`. Fields: `releaseCheck`, `outcome` (`published`), `focusCount`, `timerPhase`, `timerRunning`. This proves Foundation numeric values passed the native validator and the iPhone published the snapshot. A paired-device check must also show `Watch capture ingested` for an incoming text capture to exercise the reverse transport bridge. No task content or identifier is logged. (#1175)
- **`v1.3.0/watch-snapshot`** — `apps/mobile/hooks/root-layout/use-root-layout-watch.ts`, when Watch connectivity activation or application-context publication fails. Messages: `Watch connectivity activation failed` or `Watch snapshot publication failed`. Fields: `releaseCheck`, `outcome` (`activation-failed` | `failed`). These content-free warnings distinguish activation from publication failure; successful native acceptance uses `v1.3.0/watch-numeric-bridge`. (#1175)
- **`v1.3.0/mobile-file-activation-absent-blob`** — `apps/mobile/lib/attachment-sync-backends/file.ts`, in the activation-probe branch of `onDownload`, when the sync folder does not hold a keyed attachment's file. Message: `File Sync activation left an attachment the folder does not hold yet`. Fields: `releaseCheck`. A phone joining a Syncthing folder whose `attachments/` lacks a file must log this once per such record followed by core's `Sync folder activation accepted attachments the folder does not hold yet`; before the fix the phone cleared the key and refused with "the file could not be fetched from the new sync location". (feedback romantashit 2026-09-06)
- **`v1.3.0/cloudkit-zone-recreated`** — `apps/mobile/lib/cloudkit-sync.ts`, in `recreateCloudKitZone`, after the native module reported `ERR_CLOUDKIT_ZONE_GONE` (CloudKit `userDeletedZone` or `zoneNotFound`) and `ensureZone` succeeded again. Message: `CloudKit zone was deleted or cleared; recreated it and reset the change token`. Fields: `releaseCheck`. A tester who cleared Mindwtr's iCloud data must see this line once, followed by a normal `CloudKit write complete`; before the fix every cycle failed with "zone was cleared by user" forever. (feedback 5d1163be)
- **`v1.3.0/android-native-widget`** — `apps/mobile/lib/widget-service.ts`, in `updateAndroidWidgetsFromData`, after the payload JSON was handed to `modules/android-widget` and every placed widget was asked to redraw. Message: `Android widget payload published`. Fields: `releaseCheck`, `items` (how many task rows the payload carries, at most 20). No task title is logged. A tester whose widget shows stale or empty rows must have this line after each data change; the widget is now drawn by Android itself from that stored payload, so a line with `items` above zero next to an empty widget means the launcher did not redraw, not that the app did not publish.
- **`v1.3.0/android-widget-provider-compat`** — `apps/mobile/lib/widget-service.ts`, in `updateAndroidWidgetsFromData`, after the native module redraws at least one Tasks widget still bound to the provider component shipped in 1.2.7. Message: `Legacy Android Tasks widgets refreshed`. Fields: `releaseCheck`, `legacyWidgetCount` (number of placed widgets using the historical provider). A tester who upgrades with an old widget already placed must see this line with a count above zero and the widget must continue to update; no task content or widget identifier is logged.
- **`v1.3.0/cloud-capture-audio-retained`** — `apps/cloud/src/server-capture.ts`, in failed capture cleanup when the canonical document already references the generated audio, cannot be read, or storage authority cannot be re-established. Message: `Capture audio retained after failed document write`. Fields: `releaseCheck`, `retentionReason` (`referenced` | `unreadable` | `storage-authority`). A self-hoster who receives a capture error must see this line when the server deliberately keeps the recording rather than risk deleting bytes a published or uncertain task document may reference. No transcription, audio content, token, path, namespace, attachment id, or cloud key is logged. Cloud server logs bypass core's sanitizer, so `retentionReason` is deliberately excluded from `release-diagnostics-fields.test.ts`, like `tokenScope` and the other cloud-only fields.
- **`v1.3.0/sqlite-final-tombstone-expiry`** — `packages/core/src/sqlite-adapter.ts`, after an empty snapshot commits the physical deletion of the final retained entity tombstones. Message: `SQLite final tombstone expiry persisted`. Fields: `releaseCheck`, `count`, `outcome` (`pruned`). This proves the adapter verified that every persisted entity row was an unchanged, observed tombstone beyond the default retention window and committed the cleanup. No entity content or identifier is logged.
- **`v1.3.0/android-widget-checkoff`** — `apps/mobile/lib/pending-captures.ts`, in `applyPendingCompletion`, once per `kind: complete` queue item the app drains (written by the widget's ring tap after its undo window). Message: `Widget check-off ingested`. Fields: `releaseCheck`, `outcome` (`completed` = the store marked the task done, `already-done` and `missing` = no-op, file removed). A tester who checked a row off on the widget must see one line on the next app start or foreground; `outcome=completed` with the task still open afterwards would point at the store, `missing` at a stale widget payload. (#1173)



## v1.2.8 (carried forward, unconfirmed; keep the original tag until confirmed)

- **`v1.2.8/file-activation-absent-blobs`** — `packages/core/src/sync-run.ts`, in `prepareRemoteWriteData` after `assertActivationAttachmentsProven`. Message: `Sync folder activation accepted attachments the folder does not hold yet`. Fields: `backend` (`file`), `deferred` (count), `ids` (first five attachment ids). Tester's log: a fresh desktop joining a Syncthing or mounted folder whose `attachments/` has not arrived now shows this line and the switch completes, where 1.2.7 logged `Sync failed … Candidate attachment proof failed for <id>` and left the previous sync settings active; the ids listed must later download on a normal cycle (in-app feedback, 2026-09-04).

### Existing diagnostics still awaiting confirmation

- **stale fence after abort** (0bf5784c0) — the existing `Sync follow-up scheduled` line now carries the delay that actually applies; a tester's log must no longer show `Remote sync location is reserved; retrying after the lease lapses` counting down on every device after one phone left the app mid-cycle.
- **#1151 activation refusal names the file** (61fcfb427) — the existing `Sync failed` line carries the refusal text, which now names the attachment file, its task or project, and the cause.
- **#1001 canonical reads and the inert backstop** (2c65cb62f, 44f6de358, 9f62fbb0a) — the existing `Sync local reconcile` line with `reconcile: 'aligned-skip'` proves a local-only upload skipped the merge; a desktop log must no longer show a canonical-read failure after an MCP write.
- **#1001 Android attachment throttle while timers are paused** (c9541342e) — the existing `Mobile background sync finished` line under General carries the run duration; a background run that uploads two or more attachments must finish inside the job allowance.
- **iCloud Atomic failure** (b5200e090, bb09c2463) — the CloudKit sync error shown on the Sync screen and written to the log now names the record and reason CloudKit actually rejected instead of a bystander's `Atomic failure`.

## v1.2.7 (carried forward, unconfirmed; keep the original tags and trim once each fix is confirmed)

- **`v1.2.7/sync-settings-activation-mobile`** — `apps/mobile/components/settings/use-sync-settings-transport-actions.ts` (in `handleSync`, guarded on an explicit `options.backend`, which only an activation passes). Message: `Sync backend selected; running the verification sync to activate it`. Fields: `backend`, `cloudProvider`. Tester's log: one line the moment a complete backend is chosen or saved, before any manual Sync tap. Desktop's equivalent is the existing line under **General**.

- **`v1.2.7/sync-status-published`** — `apps/mobile/lib/sync-service.ts`, in `finalizeSuccess`. Message: `Sync status published to the store`. Fields: `backend`, `statusPublished` (`wrote-local` or `unchanged`), `lastSyncAt`, `lastSyncStatus`. Tester's log: one line per successful cycle whose `lastSyncAt` advances, proving the Sync screen's "Last sync" is no longer frozen.

- **`v1.2.7/dropbox-signin-detached`** — `apps/desktop/src-tauri/src/sync.rs` (Rust `log::info!`/`log::warn!`, target `sync`). Three phases: `phase=opened` after `open::that_detached` returns, `phase=callback-state-matched` when the real callback is accepted, `phase=callback-state-mismatch` when a stale tab replays an old state and is ignored. Tester's log: `opened` must appear immediately, not after the browser closes, and a completed connect ends in `callback-state-matched`.

- **`v1.2.7/incoming-link-redelivery`** — `apps/mobile/hooks/use-incoming-url.ts`. Message: `Incoming link delivered`. Fields: `scheme`, `host` (nothing further: a capture link carries the task title in its path and query), `delivery` (counter), `deduped`. Tester's log: pressing the same shortcut twice writes two lines with `deduped=false` and rising `delivery`; only an immediate launch-URL echo shows `deduped=true`.

- **`v1.2.7/fence-artifact`** — `apps/mobile/lib/sync-service.ts`, in `acquireWebdavRemoteMutationFence`. Message: `WebDAV sync fence artifact resolved`. Field: `artifact` (basename only). Tester's log: `artifact` must read `.mindwtr-sync-fence-v1.json` and must NEVER read `data.json` (#1132).

- **`v1.2.7/calendar-spanning-events`** — `apps/mobile/lib/external-calendar.ts`, at the end of `fetchSystemCalendarEvents`. Message: `Device calendar events loaded for the window`. Fields: `platform`, `total`, `multiDay`, `allDay`, `spanning`. Tester's log: on Android a month containing a multi-day event must report `spanning` greater than zero, which is exactly what the old containment query dropped.

- **`v1.2.7/cycle-unchanged-skip`** — `packages/core/src/sync-run.ts`, `logUnchangedCycleSkips`, called from both unchanged returns. Message: `Sync cycle changed nothing; passes skipped`. Fields: `backend`, `check` (`fast` or `read`), `skipped` (comma list). Tester's log: an idle device shows this after every `Sync fast check found no changes`, with `local-persist,store-refresh,attachments,remote-fence`. The field is `skipped`, NOT `skippedPasses`, which the sanitizer redacts.

- **`v1.2.7/daily-attachment-presence`** — `apps/desktop/src/lib/sync-service.ts`, in `hasAttachmentSyncWork`. Message: `Attachment presence re-verification checked`. Fields: `presenceDue`, `hasScope`. Tester's log: `presenceDue=false` on every cycle within a day of the last full pass, and `presenceDue=true` once a day or after the sync location changes.

- **`v1.2.7/android-attachment-link-fallback`** — `apps/mobile/lib/attachment-file-installer.ts`, after the native installer reports that Android refused the hard-link publication and used the exclusive-copy fallback. Message: `Android attachment generation published with exclusive-copy fallback`. Fields: `publication` (`exclusive-copy`). Tester's log: one line with `publication=exclusive-copy` proves the #1139 fallback succeeded instead of relying on the absence of the old `Could not publish attachment generation (EXDEV)` failure.
