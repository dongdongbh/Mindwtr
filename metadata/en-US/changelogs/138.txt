Adds Reference to every task's status list. Stops a phone syncing every few seconds after another device deleted attachments or a desktop wrote the data, releases the sync lock after an interrupted sync, and names the file when a sync location cannot be verified. Sync fails over from a dead IPv6 address in ten seconds, and background attachment uploads no longer stall. Week view opens on five days, widgets show the same next action as the app, and translations are corrected.
